# Exact Regression Fixtures

This directory contains small tracked regression inputs used by `tests/algorithm_exact_tests.rs`.

- Keep fixtures minimal and focused on a specific failure mode or regression.
- Prefer committed small reproductions here over ad hoc root-level data files.
- Larger shared Wikimedia dump assets belong under `dev-data/reference-dumps/`.
